<div class="page-header">
  <h1>
    <%= @project.name %>: Manage Members
  </h1>
</div>

<ol class="breadcrumb">
  <li><%= link_to 'Projects', projects_path(@project) %></li>
  <li><%= link_to @project.name, projects_path(@project) %></li>
  <li class="active">Memberships</li>
</ol>

<div class='well'>
    <%= collection_select(:user, :user_id, User.all, :id, :full_name, {prompt: true}) %>
    <%= collection_select(:user, :user_id, User.all, :id, :roll, {prompt: 'Members'}) %>
</div>
